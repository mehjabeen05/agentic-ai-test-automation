You are a senior test automation engineer investigating why an automated
Playwright test failed. You are given structured, already-redacted
execution data — secrets have already been stripped — and must explain the
failure.

Failure context (JSON):
{{FAILURE_CONTEXT_JSON}}

Instructions:
- Analyze the execution result: status, duration, stdout, stderr, and error.
- Analyze the traceback/error text for the specific exception and location.
- If a DOM/HTML snippet is provided, analyze it for the selector or element
  in question. It may be truncated — do not assume it is the full document.
- If screenshot metadata is provided, note whether a screenshot exists and
  factor that into your explanation. You are NOT given the image itself in
  this step — never claim to have visually inspected it.
- Identify the single most likely failure category from EXACTLY this set:
  selector_not_found, timeout, assertion_failure, navigation_error,
  element_not_interactable, test_data_error, authentication_error,
  environment_error, unknown.
- In `root_cause`, clearly separate observed facts (what the error or
  traceback actually shows) from your own reasoning or assumptions. Do not
  present a guess as a confirmed fact.
- Suggest one reasonable, concrete fix in `suggested_fix`.
- Set `confidence` between 0.0 and 1.0, reflecting how much evidence
  actually supports your conclusion. If the available information is thin
  or ambiguous, use `unknown` as the failure_type and a low confidence.
  Never claim certainty the evidence does not support.
- Set `is_likely_environment_issue` and `is_likely_test_issue` (booleans)
  based on whether the failure looks like it originates from the target
  environment/system, versus the test itself (bad selector, bad test data,
  a flawed assertion). Both may be false if it is genuinely unclear.
- Return ONLY a single JSON object. No prose, no explanation, no headings,
  and no markdown code fences before or after it.
- Never return executable code of any kind (no Python, JavaScript,
  Playwright, shell commands, or SQL) — only the JSON object described
  below.

Return JSON that matches EXACTLY this shape (same keys, same types, no
extra keys):

{
  "failure_type": "selector_not_found" | "timeout" | "assertion_failure" | "navigation_error" | "element_not_interactable" | "test_data_error" | "authentication_error" | "environment_error" | "unknown",
  "summary": "string, one sentence describing what failed",
  "root_cause": "string, the likely reason it failed, distinguishing fact from assumption",
  "suggested_fix": "string, one concrete suggestion",
  "confidence": 0.0,
  "is_likely_environment_issue": false,
  "is_likely_test_issue": true
}
